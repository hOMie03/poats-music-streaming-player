{
  "dependencies": {
    "vercel": "^34.2.7"
  },
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run start",
    "vercel-build": "npm run client && npm run server"
  }
}
